#include "main.h"
#include <stdio.h>

/**
 * main - Add two integers and returns the result.
 * @i: print int i
 * @k: print int k
 * Return: Always 0 (Success)
 */
int add(int i, int k)
{
return (i + k);
}
